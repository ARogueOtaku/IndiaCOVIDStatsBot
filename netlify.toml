[build]
    functions = "endpoints"
    command = "sudo apt-get install libuuid-devel libmount-devel && sudo apt-get update && cp /lib64/{libuuid,libmount,libblkid}.so.1 node_modules/canvas/build/Release/ && netlify-lambda install"